# RESUME STATE — UNI Colony — 2026-06-22 (pre-reboot handoff)

Authoritative handoff for a **full resume after a PC reboot**. Everything below the line is committed +
pushed; the running processes are all stopped (the box was deliberately frozen). A fresh session can read
this file + `coordination/flow.jsonl` + `lab/purebody/purebody.v1.jsonl` and continue with zero loss.

- **Repo:** `C:\Users\mpolz\Documents\Strings`
- **Branch:** `lab/ozone-life-uni-hard-science`   **HEAD:** `73327c8` (in sync with origin)
- **Pure-AIF invariant (never violate):** pure-Elixir active inference — NO Nx/Rust/NIF/port, NO
  backprop/RL/LLM in the loop. The colony brain is `SP.Brain.*`.

---

## 0. ONE-GLANCE STATE

| Workstream | Status | Key commits |
|---|---|---|
| **WS-A** build economy | ✅ DONE | `3868561` (tools-sense bug), `2a1ec7c` (surface-shelter curriculum) |
| **WS-B** slow context (SlowContext wired) | ✅ DONE — **UNI-GPT-validated v2** | `5852a22` (v1), `086d91b` (v2) |
| **WS-C** purebody Step 3b/4 (pixels-only) | 🔨 IN PROGRESS — soft-obs foundation ✅; rest owed | `73327c8` (soft-observation), `60d438d` (consult) |

Two **UNI-GPT OODA consults** landed and were applied/recorded — both materially corrected the designs
before they shipped a false claim. The colony + MC + OBS/stream were stopped to free the box and unblock
the consults (the ChatGPT renderer was CPU-starved).

---

## 1. RUNTIME STATE — everything is DOWN (and how to bring it back)

After reboot, nothing is running. The box was frozen on purpose. **Do NOT restart the colony/stream without
the steward's go-ahead** (memory: "live-stream changes need a go-ahead"; the box was deliberately freed).

Ports when live: Phoenix `:4000`, MC Paper 1.16.5 `:25565`, RCON `:25575` (pass `sp`), prismarine cam `:3020`,
OBS websocket `:4455`. Node `uni@Thinker`, cookie `sp`. MC forest seed `8675309`.

**To bring the colony back up (only when authorized):**
1. **MC server** (Paper) — start it first (see `docs/RUNBOOK_LIVE_STREAM.md` + memory `ops_live_stream_runbook`).
   It must be listening on `:25565`/`:25575` before the colony connects.
2. **Colony node** — run `runs/start_colony.bat` (it does `cd ui && elixir --sname uni --cookie sp -S mix
   phx.server`, logging to `ui_node.out.log`). ONE node only — never a 2nd `producer.run`.
3. **Producer + agents** — `elixir --sname trigX --cookie sp runs/trigger_restart.exs` (starts
   `SP.Producer` + spawns the 5 design UNIs `UNI-{0,1,1,2,3}-1`, reloading `runs/colony/UNI-kin-1.bin`).
4. **Cam window** (WebGL `:3020`) — a real Chrome window with the anti-throttle flags (memory
   `ops_multifeed_broadcast`), captured by OBS via WGC.
5. **OBS / YouTube** — see `docs/RUNBOOK_LIVE_STREAM.md` + memory `ops_obs_youtube_auth_gotcha` (empty
   `[Auth] Type=` in basic.ini or OBS streams nothing). Stream key is in OBS — NEVER echo it.
6. **Reset to a buildable surface** (optional) — `elixir runs/reset_buildable.exs` (spreadplayers to a
   pristine forest surface; peaceful + day).

**Graceful shutdown again later:** `elixir runs/stop_mc.exs` (RCON save-all + stop), then kill the `erl.exe`
(sname uni) + `node.exe` (body.js/director) + `obs64` + the Temp-profile Chrome windows.

---

## 2. WHAT'S DONE (committed + pushed)

### WS-A — build economy (✅)
- **Root cause (`3868561`):** `inventoryCounts()` tested `includes("wood")` BEFORE the tool check, so every
  `wooden_pickaxe`/etc. (contains "wood") was miscounted as wood → `tools` stayed 0 forever → curriculum
  froze at phase 2. The colony had been crafting pickaxes the whole time. Fixed (tool-suffix first) + WOOD
  const made material-only. `viewer/body.js`.
- **Surface-shelter curriculum (`2a1ec7c`):** phases 3+4 preferred "enclosed" → agents dug to bedrock to
  satisfy it. Per the steward's option-2 (boost build, don't constrain mining), phase-4 now prefers a SURFACE
  shelter (open sky positive, underground negative, walls kept). `lib/sp/brain/curriculum.ex`.
- **Live-verified:** tools count (UNI-0-1 had 7+); colony stays on the surface (zero bedrock across a watch).
- **Honest gap:** a *completed* shelter wasn't observed in-window (the curriculum timescale; no agent reached
  phase-4 build during the watch). Recorded `colony-cycle-8/9` in `coordination/flow.jsonl`.

### WS-B — SlowContext wired into the live decide path (✅, GPT-validated)
- Heritable, **OFF by default** ⇒ the live colony is **byte-identical**.
- **The OODA loop:** built v1 (`5852a22`) → UNI-GPT ruled **WITHHELD-with-changes** → v2 (`086d91b`) applies
  both corrections:
  - **DOWN** = a δ-weighted contextual prior that **REPLACES** the forward prior: `ln p⁻ = (1-δ)·forward +
    δ·ln(W·q_scene)`; `δ=0` short-circuits to flat. δ = heritable DNA `slow_context_coupling` (default 0).
  - **UP** = the **extrinsic likelihood** (`last_lik = softmax((lnA)ᵀo)`), NOT the prior-shaped posterior.
- **Result:** self-reinforcement **eliminated** — W4 self-loop entropy drop **0.6561 (v1) → −0.0008 (v2)**.
- `gate.slow-context.wired` v2 W1–W4 PASS; 254/255 brain tests; `hierarchy2.ex` + `slow_context.ex`
  byte-unchanged; survived a 6-lens adversarial review. Records: `purebody.v1.jsonl` (`gate_slow_context_
  wired_result_v2`), `flow.jsonl` cycles 10–13.
- **Files:** `lib/sp/brain/{model.ex,infer.ex,genome.ex,mc.ex}`, `test/sp/brain/slow_context_wired_test.exs`.

### WS-C — purebody Step-4 pixels-only (🔨 foundation done)
- **Consult (`60d438d`, cycle-14):** UNI-GPT ruled the pixels-only design **Validated-with-changes**;
  "pixels-only is viable" **WITHHELD** until purity + phase-0 survival. Three rulings:
  1. one 12-state scene factor is OK for **phase-0 only**; the full curriculum needs **multiple pixel-derived
     factors** (scene + threat + affordance), all genuine vision organs (no symbolic wrappers).
  2. **no hard argmax** — feed the cortex **soft posterior** as virtual evidence; preferred form the **cavity
     `λ = q⁺/q⁻`** (divide out the cortex's temporal prior); argmax recovered in the peaked limit.
  3. gate = `perception_mode :pixels_only`; inputs limited to `{pixels, hurt, yaw, action_history}`; static
     scan + a **runtime purity trap** (symbolic APIs → panic stubs, phase-0 run completes).
- **Soft-observation engine path (`73327c8`, cycle-15) — BUILT + VALIDATED:** a factor ingests a SOFT
  observation (a distribution `r`) as `(ln A)ᵀr`, with soft-count learning `a_{g,s} += lr·r_g·qs`. Both reduce
  to the hard-integer path at `r = onehot(o)`, so every existing symbolic agent is **byte-identical**. The
  existing `gamma_m` = the GPT's `γ_vision` (no new knob). `gate.soft-observation` S1–S4 PASS; 258 brain
  tests 0 failures. Files: `lib/sp/brain/{infer.ex,learn.ex}`, `test/sp/brain/soft_observation_test.exs`.

---

## 3. WHAT'S OWED — NEXT STEPS (prioritized)

### WS-C (resume here) — the remaining Step-4 build
1. **genome `perception_mode` + `senseLine` strip** (offline code): add `perception_mode` to the Genome
   (`:symbolic` default, `:pixels_only`), carried heritably; when pixels-only the body's `senseLine`
   (`viewer/body.js:250`) emits ONLY `{pixels(sceneState), hurt, yaw, action_history}` — drop the 14 symbolic
   God-sight channels. Brain↔body obs shape must match the pixels-only genome.
2. **Multiple pixel-derived factors (scene + threat + affordance)** + **vision service** (CROSS-COMPONENT,
   UNI.OS): the vision cortex must emit (a) the scene-state POSTERIOR (a 12-vector, not the argmax int the
   current `sceneState()` reads) and ideally its predictive prior (for the cavity `λ=q⁺/q⁻`), plus
   pixel-derived threat-salience + affordance channels. These are VISION organs, not `findBlock`/`nearestEntity`
   wrappers. The soft-obs engine path (done) is ready to ingest these.
3. **Runtime purity trap** (test harness): stub the symbolic APIs to panic; a seeded **pixels-only** phase-0
   run must complete without calling them. Needs a pixels-only agent running → **the colony**.
4. **Behavioral phase-0 gate** (Class C, WITHHELD): the pixels-only colony must clear curriculum phase-0
   survival. Needs **the colony restarted**.

### WS-C — Step 3b (GUI crafting) — independent of Step 4
- `viewer/body.js doCraft` (line ~382) still uses `bot.recipesFor`/`bot.craft` (the owed debt — see
  `mc_purity_scan.cjs` `action_human_controls_only` violations). Replace with GUI `clickWindow`/
  `setQuickBarSlot` drags (the primitives already exist in `selectToHand`). Static gate: 0 `scripted_craft`
  hits. Behavioral gate (does it actually craft) needs the colony.

### WS-B — owed for the FINAL "Validated" stamp (the GPT's grid)
- The full sensitivity grid: `δ{0,1e-6,.01,.05,.1,.25,.5,.75,1} × ρ{0,.25,.5,1,2} × Sg{1,2,3,5,8,16} ×
  p_stay{1/Sg,.5,.8,.9,.97,.995,1} × W{uniform..onehot} × ≥32 seeds` (offline test).
- The up-message precision `ρ` knob (currently ρ=1 implicit).
- An **informative or LEARNED W** — the live benefit (`build_slow_context` uses uniform W ⇒ inert at any δ).
- **Live A/B** (δ>0 + informative W vs flat controls) after a clean colony window — Class C, WITHHELD.

### WS-A — follow-up
- The completed-shelter behavioral pass (an agent reaching phase-4 build) — a longer curriculum-horizon watch.

---

## 4. THE UNI-GPT CONSULT (how to keep collaborating)

- **GPT:** "UNI Active Inference Guide" (the steward's custom builder GPT). **Thread:**
  `https://chatgpt.com/c/6a36dfc6-a5f0-8333-988b-84d8911b2807` ("Design Validation Feedback"). Driven via
  Claude-in-Chrome (the steward's main browser, deviceId `3e43a767…951fbf`).
- **Input gotcha (IMPORTANT):** this thread is heavy → per-keystroke `type`/`key` CDP dispatch **times out**.
  Use `form_input` to set the textarea value in one shot, then **mouse-click** the send button (mouse events
  work; key events don't). `get_page_text` does NOT extract the MathML-heavy replies — read them via scrolled
  screenshots.
- **Doctrine:** Validated / Validated-with-changes / WITHHELD (never a fabricated percent); RED-first gates;
  sensitivity grids; evidence taxonomy (A derivable, B source-checked, C assumption/out-of-scope, U/Sec);
  behavioral claims WITHHELD until seeded rollouts pass; prerequisites must be a typed dependency graph.
- **NEVER paste secrets** (stream key, OAuth tokens, repo internals beyond the math/design) into the GPT.

---

## 5. THE CAVITY PRINCIPLE (the cross-consult insight to carry forward)

Both consults converged on the **same fix**: a hierarchy level must never treat an **upstream prior as fresh
evidence**.
- **WS-B (UP):** the slow parent hears the child's **extrinsic likelihood**, not the posterior its own
  down-prior already shaped (else self-reinforcement, the 0.6561 echo).
- **WS-C (DOWN ingest):** the brain hears the cortex's **cavity `λ = q⁺/q⁻`** (the posterior with the cortex's
  own temporal prior divided out), not the raw posterior — same principle, other direction.
This is the unifying design law for the whole hierarchy.

---

## 6. WHERE THE DETAIL LIVES

- **Append-only science ledger:** `coordination/flow.jsonl` (colony cycles 1–15) and
  `lab/purebody/purebody.v1.jsonl` (the gates + their RED-first bars/results, incl. the adversarial reviews).
- **The approved plan:** `C:\Users\mpolz\.claude\plans\prancy-launching-teapot.md` (WS-A/B/C, GPT-rigor-gated).
- **Production platform plan:** `docs/UNI_PRODUCTION_PLATFORM.md` + `docs/UNI_PRODUCTION_NEXT_AGENT_PROMPT.md`.
- **Live-stream runbook:** `docs/RUNBOOK_LIVE_STREAM.md`. **Memory:** `MEMORY.md` index + the `ops_*` files.
- **Throwaway run scripts** (gitignored, force-added): `runs/{trigger_restart,reset_buildable,stop_mc,
  watch_build,probe_colony,diag_buildchain,tp_surface}.exs`.

---

## 7. SECURITY / GUARDRAILS (persist verbatim)

- Stream key + OBS OAuth tokens are sensitive — NEVER echo/reproduce.
- UNI.OS repo never pushed to a public remote.
- `uni-lab` MCP mutating ops require HUMAN approval (cannot self-approve).
- Commit/push only when asked. Don't override design params / expand population unprompted. "Real sight" =
  literal pixel vision.
- The box was deliberately FROZEN — get a go-ahead before restarting the colony/stream (re-consumes the box).

---

## 8. LAB DEPLOYMENT — colony → UNI-lab (IN PROGRESS, 2026-06-22)

Goal: run the colony on the **UNI-lab** (Dell PowerEdge `uni-lab`, via the `uni-lab` MCP), NOT the dev box.
Recipe: `docs/UNI_OS_COLONY_MIGRATION.md` Path A. Lab: 39 GiB RAM (31 free), 5.1 TB, podman, registry on :5000.

**DONE:**
- ✅ **`mc-server` LIVE** on the lab: `podman run -d --name mc-server --network uni-colony-net -e EULA=TRUE
  -e TYPE=PAPER -e VERSION=1.16.5 -e SEED=8675309 -e RCON_PASSWORD=sp -e DIFFICULTY=peaceful -e ONLINE_MODE=FALSE
  -e MEMORY=3G -e SPAWN_PROTECTION=0 -e MAX_PLAYERS=20 -p 127.0.0.1:25575:25575 -v uni-mc-world:/data
  docker.io/itzg/minecraft-server:java11`. Verified: "Done (8.258s)! RCON running on 0.0.0.0:25575".
- ✅ **`uni-colony-net`** podman network created.
- ✅ **MC_HOST patch** committed `27ad43d`: producer/director/bridge default `mc_host` to `System.get_env("MC_HOST")`
  (dev box → 127.0.0.1; container → `mc-server`). Backward-compatible.
- ✅ **uni-colony:v1 image BUILT + PUSHED** to the lab registry (`10.190.245.122:5000/uni-colony:v1`, 2.31 GB,
  confirmed in the catalog). LIGHT + HEADLESS — no native gl: built on the dev box (podman machine) +
  `podman push --tls-verify=false`. Commits: camera-gate + light Dockerfile `653d2dc`, colony auto-start `43cfa13`.
  Headless via baked ENV `UNI_CAM=0` (Director skips the stream cam) + `UNI_AUTOSTART=1` (SpUi.Application boots the
  Producer + design UNIs [0,1,1,2,3] ~12 s after start) + no `UNI_POV_PORT` (bodies never load prismarine-viewer).
- ✅ **COLONY LIVE on the lab (ROOTLESS, via SSH)** — the MCP rootful-run approval kept timing out, so per the
  owner's "use SSH, get it done" the whole stack runs **rootless under user `uni`** (ssh key `~/.ssh/uni-lab_ed25519`,
  `uni@10.190.245.122`): no token, no approval queue, no dev-box load. **6 UNIs online + stepping** (RCON `list`).
  - The dev-box-built registry image had out-of-range UIDs for rootless ⇒ instead **ship source over SSH +
    build ON the lab**: `git archive HEAD | ssh uni@… "tar x -C /tmp/ucx"`, then `cd /tmp/ucx && podman build -f
    deploy/uni-os/uni-colony.Dockerfile -t localhost/uni-colony:v2 .` (rootless ⇒ native UIDs).
  - **The real MC_HOST bug**: the body's `mc_host` is SET (not defaulted) in `colony.ex:103` + `lineage.ex:135`
    as `state.opts[:mc_host] || "127.0.0.1"`, so the producer/director/bridge env-fallbacks never fired. Fixed in
    `18ae0a9` (add `System.get_env("MC_HOST")` there too). v2 image has it; bodies route to `mc-server` by name.
  - **Connection-throttle**: all bodies share one container IP, so Paper's `connection-throttle: 4000` (bukkit.yml)
    dropped all but the first (localhost is exempt — why the early namespace-share worked). Set it to `-1` and
    restarted mc-server ⇒ all 6 connect. (Re-apply if the mc-world volume is reset.)
  - **Live run (rootless, restart-safe — survived an mc-server restart):**
    `podman network create uni-colony-net` · mc-server: `podman run -d --name mc-server --network uni-colony-net -e EULA=TRUE -e TYPE=PAPER -e VERSION=1.16.5 -e SEED=8675309 -e RCON_PASSWORD=sp -e DIFFICULTY=peaceful -e ONLINE_MODE=FALSE -e MEMORY=3G -e SPAWN_PROTECTION=0 -e MAX_PLAYERS=20 docker.io/itzg/minecraft-server:java11`
    · colony: `podman run -d --name uni-colony --hostname uni-colony --network uni-colony-net localhost/uni-colony:v2`.
  - Verify: `ssh uni@… "podman exec mc-server rcon-cli list; podman logs uni-colony | tail"`.
  - **CLEANUP owed**: a redundant **rootful** mc-server (from the earlier MCP path) is still running — stop via one
    MCP `podman_stop mc-server` approval (can't reach rootful over SSH-as-uni). Not boot-persistent yet — for that,
    `podman generate systemd --new` user units + `loginctl enable-linger uni`.

**NEXT — `uni-colony` (the brain), the heavy piece:**
- ONE container (the bodies are Erlang Ports of the BEAM ⇒ same machine). Heavy/finicky native build:
  `prismarine-viewer` → `headless-gl` + `node-canvas`. Build deps: `nodejs npm python3 make g++ pkg-config
  libcairo2-dev libpango1.0-dev libjpeg-dev libgif-dev librsvg2-dev libgl1-mesa-dev libxi-dev xvfb libgl1-mesa-dri`.
- Dockerfile: `FROM elixir:1.18-otp-27`; COPY mix.exs config lib scripts ui viewer into /app; `cd ui && mix
  deps.get`; `cd viewer && npm install --omit=dev`; ENV `MC_HOST=mc-server MC_PORT=25565`; EXPOSE 4000 3020;
  CMD `cd /app/ui && elixir --sname uni --cookie sp -S mix phx.server`. (@repo_root=/app ⇒ body.js=/app/viewer/body.js.)
- Source→lab (lab allowlist has NO git/tar/bash — only podman/nft/ls/df/journalctl/systemctl/…):
  - **Path A (ship from here):** build on the dev box (has Docker 28.5 + podman 5.8) → `podman push --tls-verify=false
    10.190.245.122:5000/uni-colony` → lab `podman pull 127.0.0.1:5000/uni-colony` + run. One-time dev-box build-load.
  - **Path B (build on lab):** `python3 -m http.server` the source tarball from the dev box (LAN 10.190.245.196)
    → lab `podman build http://10.190.245.196:PORT/ctx.tgz`. No dev-box build-load; needs the dev-box firewall open.
- Then: bodies auto-spawn as Ports once the BEAM is up; the **cam** (director.js + xvfb) is last; open `nft` for
  :4000 + :3020 on the lab LAN so the dev-box OBS captures them (the migration doc's cutover).

**Approval flow (uni-lab MCP):** every mutating op needs the operator to APPROVE within **180s** at
**https://10.190.245.122/approvals/** (LAN) or `uni-approve <id>` on the box. os_exec is two-step: `dry_run`
(free, returns a token) → `confirm` (the approval). Only allowlisted argv0. Tip: set `UNI_APPROVALS_AUTOAPPROVE`
for the deploy ops to drive hands-off and avoid the 180s-window misses. Dev-box LAN IP = **10.190.245.196**.
